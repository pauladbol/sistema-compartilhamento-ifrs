<%= form_for(@post) do |f| %>
	<% if @post.errors.any? %>
		<div id="error_explanation">
		  <h2><%= pluralize(@post.errors.count, "error") %> prohibited this post from being saved:</h2>

		  <ul>
		  <% @post.errors.full_messages.each do |message| %>
			<li><%= message %></li>
		  <% end %>
		  </ul>
		</div>
	<% end %>

	<div class="form-group">
		<div class="col-lg-offset-3 col-lg-6">
		  <%= f.text_field :title, :id => "title-post", :placeholder => "Título", :class => "form-control" %>
		</div>
	</div>
	<div class="form-group">
		<div class="col-lg-offset-3 col-lg-6 text-area-size">
		  <%= f.text_area :text, :id => "text-post", :placeholder => "Texto", :class => "form-control", cols: "30", rows: "9"  %>
		</div>
	</div>
	<div class="form-group">
		<div class="col-lg-offset-3 col-lg-6">

		  	<%= f.collection_select :subject_id, Subject.order(:name), :id, :name, :class => "form-control", :prompt => "--- Assunto ---" %>
		</div>
	</div>

	<%= f.hidden_field :user_id, :value => 1 %>
	<div class="form-group">
		<div class="col-lg-offset-3 col-lg-6">
		  <%= f.submit "Salvar", :class => "btn btn-default btn-lg btn-block btn-primary" %>
		</div>
	</div>
<% end %>
