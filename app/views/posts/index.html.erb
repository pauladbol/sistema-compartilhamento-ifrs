<% if notice != nil %>
  <div class= "alert alert-success col-lg-offset-4 col-lg-4 centralize text-center">
    <p id="notice"><%= notice %></p>
  </div>
<%end%>

<section id="section-posts">
  <div class="form-horizontal">
      <div class="col-lg-12 text-center login-title">
          <h1>Postagens</h1>
        </br>
          <h4></h4>
      </div>

  <div class="col-lg-offset-2 col-lg-8">
      <table class="table table-hover table-responsive">
        <thead class="thead-default">
          <tr>
            <th>Título</th>
            <th>Categoria</th>
            <!-- <th colspan="2"></th> -->
          </tr>
        </thead>

        <tbody>
          <% @posts.each do |post| %>
            <tr>
              <td><%= link_to post.title, post %></td>
              <td><%= post.subject.name %></td>
              <!-- <td><%= button_to 'Show', @post %></td> 
              <td><%= button_to 'Edit', edit_post_path(post) %></td>
              <td><%= button_to "Delete", post, :method=>:delete, :class=>:destroy %> </td>-->
            </tr>
          <% end %>
        </tbody>
      </table>
  </div>

  </div>

</section>